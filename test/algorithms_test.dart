import 'package:test/test.dart';

import '../bin/algorithms.dart';

void main() {
  test('calculate', () {
    expect(calculate(), 42);
  });
  test('calculat', () {
    expect(calculate(), 42);
  });
  test('calcula', () {
    expect(calculate(), 42);
  });
  test(
    'calcu',
    () {
      expect(calculate(), 42);
    },
  );
  test('calcu', () {
    expect(calculate(), 42);
  });
  test('calculate', () {
    expect(calculate(), 42);
  });
}
